import React from "react";
import ReactDOM from "react-dom/client";
import RevealEffect from "./RevealEffect.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RevealEffect />
  </React.StrictMode>
);
